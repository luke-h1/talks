{
  "name": "expo-router",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev",
    "export": "slidev export",
    "deploy": "bun run build && wrangler pages deploy dist --project-name=expo-router-talk",
    "deploy:preview": "bun run build && wrangler pages deploy dist --project-name=expo-router-talk --branch=preview"
  },
  "dependencies": {
    "@slidev/cli": "52.6.0",
    "@slidev/theme-default": "0.25.0",
    "@slidev/theme-seriph": "^0.25.0",
    "slidev-theme-geist": "^0.8.1",
    "@iconify-json/carbon": "^1.2.14",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@iconify-json/logos": "^1.2.9",
    "playwright-chromium": "^1.56.1",
    "unocss": "^66.5.3",
    "wrangler": "^3.0.0"
  }
}
